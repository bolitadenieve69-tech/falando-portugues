# Lightweight dependencies for CI and local test runs.
#
# The real-time voice stack (pipecat-ai) is stubbed in tests/conftest.py, so it
# is intentionally NOT installed here — that keeps CI fast and avoids pulling in
# torch/transformers. Runtime floors mirror requirements.txt.
anthropic>=0.25.0
livekit-api>=0.6.0
python-dotenv>=1.0.0
fastapi>=0.111.0
httpx>=0.27.0
slowapi>=0.1.9
limits>=3.13.0
aiosqlite>=0.20.0
bcrypt>=4.1.0
pytest>=8.0.0
pytest-asyncio>=0.23.0
